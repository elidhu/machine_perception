#!/bin/bash

# this is just so I don't have to pip install every time I make changes to my
# utils module, I might also just add it to my bash_profile
export PYTHONPATH=/Users/kevinglasson/dev/git/mp_utils